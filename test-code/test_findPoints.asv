% Test the findPoints script

% Test data
n_points = 1000;
phase = zero(1, n_points);
shifts = [100, 200, 400, 650, 875];
n_shifts = length(shifts);

for i=1:n_shifts
    phase(shifts(i):end) = phase(shifts(i):end) + 1;
end

alpha 